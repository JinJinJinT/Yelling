for (var i = 0; i < 5; i++) {
  function timeout(val) {
    setTimeout(function() {
      console.log(val);
    }, 1000);
  }
  timeout(i);
}
//use diff updates of node
//check out play audio eith node
//maybe switch to original file playing :(
